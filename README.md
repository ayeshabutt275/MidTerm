# ☕ Coffee Shop App

A simple **React Native** app to display a coffee shop menu with options to view the full menu or get a random item. Backend is powered by a REST API.

---

## ⚡ Features

- View the **full menu** of drinks and snacks.
- Get a **random menu item** with “Surprise Me!”.
- Clean, responsive UI with **light color scheme**.
- Works on **Android/iOS** devices via React Native.

---

## 🛠️ Tech Stack

- **Frontend:** React Native, TypeScript
- **Backend:** Node.js, Express
- **HTTP Requests:** Axios
- **State Management:** React Hooks (`useState`)

### 2. Install dependencies

**Frontend:**

```bash
cd frontend
npm install
```

**Backend:**

```bash
cd ../backend
npm install
```

### 3. Run the app

**Start backend server:**

```bash
npm start
# Default port: 3000
```

**Start frontend app:**

```bash
cd ../frontend
npx react-native run-android
npx react-native run-ios
